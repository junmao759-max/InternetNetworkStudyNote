1. **原理**
    
    使用特殊控制字符标记帧边界：`SOH(帧开始)`、`EOT(帧结束)`；
    
    引入**转义字符 ESC**实现透明传输。
2. **透明传输规则**

- 原始数据出现 `SOH / EOT / ESC`，**前面额外插入一个 ESC**
- 接收方收到 ESC：删除 ESC，读取后一个原始字符
- 若连续两个 ESC：代表原始数据里本身就是 ESC

3. **缺点**：面向字节，硬件实现复杂、兼容性差。