###### 一、网桥

1. **产生背景** 集线器在物理层扩展以太网，会形成**更大的冲突域**。网桥用来分割冲突域。

> 网桥属于**数据链路层设备**，现已被交换机完全取代，新大纲已删除网桥大部分内容，了解概念即可。

2. **网桥核心功能**

- 基于**MAC 地址**对帧进行转发 / 过滤；
- 分割冲突域：网桥每个端口是独立冲突域；**广播域不能分割**（广播帧依旧向所有端口转发）。
- 网段故障隔离：一个网段故障，不影响其他网段运行。
- 自学习：记录源 MAC‑端口映射，生成转发表。