- 产生原因：通信两端**速率、缓存大小不同**；发送方速率 > 接收方，接收缓冲区溢出造成帧丢失。
- 核心目的：限制发送方发送速率，**不超出接收方接收能力**。
- 实现方式：依靠**反馈机制**，接收方反馈告知发送方：继续发送 / 暂停发送。
- OSI 与 TCP/IP 区别（高频考点）
    
    - OSI：**数据链路层**具备流量控制；控制对象：**相邻节点间链路流量**。
    - TCP/IP：流量控制放到**传输层 (TCP)**；控制对象：**端到端（源主机→目的主机）**流量。