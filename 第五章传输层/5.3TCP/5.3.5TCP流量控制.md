## 5.3.5 TCP 流量控制

> 
> 流量控制：**点对点，接收方控制发送方速率，防止接收缓存溢出**；滑动窗口，接收窗口 rwnd。

1. 接收方通过 TCP 首部窗口字段通告 rwnd，告诉发送方自己还能接收多少字节。
2. 发送窗口上限取接收窗口 rwnd；**窗口单位字节；窗口可以动态变化**。
3. 零窗口：rwnd=0，发送方停止发送；持续计时器：收到零窗口，启动计时器；超时发送**零窗口探测报文**，查询接收窗口。
4. 对比：
  - 传输层流量控制：端到端，进程之间；窗口动态可变。
  - 数据链路层流量控制：相邻两个节点。
