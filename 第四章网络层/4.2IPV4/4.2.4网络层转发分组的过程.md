1. 斜线记法 `IP/前缀长度`，不再区分 A/B/C 类。
2. CIDR 地址块：地址总数一定是 \(2^n\)；可分配主机数 \(2^n-2\)（去掉网络地址、广播地址）。
3. **路由聚合（超网）**：多个小地址块合并成一个大 CIDR 块，**减小路由表表项**，提升性能。
4. **最长前缀匹配**：路由表多条匹配目的 IP，**选择前缀最长（最具体）的路由条目转发**。
5. 两种特殊路由

- **特定主机路由**：`/32`，针对单个主机指定路由。
- **默认路由**：`0.0.0.0/0`，所有没有匹配条目的报文走默认路由。