1. 应用层协议，基于**UDP**；客户端 68 端口，服务器 67 端口。
2. 作用：动态分配 IP 地址（子网掩码、网关、DNS），地址租用。
3. 四次报文（全部广播）
4. DHCP Discover 发现（客户端寻找服务器）
5. DHCP Offer 提供（服务器给出可用 IP）
6. DHCP Request 请求（客户端确认选用地址）
7. DHCP Ack 确认（服务器正式分配 IP）

> 客户端初始没有 IP，源 IP 填`0.0.0.0`，目的广播`255.255.255.255`。